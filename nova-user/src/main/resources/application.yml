server:
  port: 8001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nova_user?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: nova123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.novamall.user.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: nova-mall-jwt-secret-key-2024-secure
  expiration: 86400000